use serde::de::DeserializeOwned;
use tauri::{plugin::PluginApi, AppHandle, Runtime};

use crate::models::*;

pub fn init<R: Runtime, C: DeserializeOwned>(
  app: &AppHandle<R>,
  _api: PluginApi<R, C>,
) -> crate::Result<Homebrew<R>> {
  Ok(Homebrew(app.clone()))
}

/// Access to the homebrew APIs.
pub struct Homebrew<R: Runtime>(AppHandle<R>);

impl<R: Runtime> Homebrew<R> {
  pub fn ping(&self, payload: PingRequest) -> crate::Result<PingResponse> {
    Ok(PingResponse {
      value: payload.value,
    })
  }
}
